node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store
.env*.local

# Source assets are large binaries — keep them out of git.
assets-source/**
!assets-source/README.md
# ...but scene SOURCE is code, not an asset — it must be versioned.
!assets-source/3d/
!assets-source/3d/*.py
assets-source/3d/*.blend
assets-source/3d/*.blend[0-9]*

reference/*.html

# rendered intermediates
assets-source/3d/_render/
.gstack/

# ...but keep the world film's story, prompts + runbook (small text) in git
!assets-source/world/
!assets-source/world/**
assets-source/world/**/*.png
assets-source/world/**/*.mp4

# third-party skill source (kept locally, not part of the site)
lets-scroll-main/
